# Historical build records

These files document earlier patch and QA rounds. They are preserved for provenance only.

They are not the current product specification, current implementation status, or current test report. The repository source, root `PRODUCT.md`, root `README.md`, and the latest successful CI run are authoritative.
